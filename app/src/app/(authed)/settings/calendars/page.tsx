"use client";

import { CalendarsSettingsTab } from "@/components/settings/calendars-tab";

/** Settings · Calendars (individual + round-robin CRUD). */
export default function SettingsCalendarsPage() {
  return <CalendarsSettingsTab />;
}
