"use client";

import { SecuritySettingsTab } from "@/components/settings/security-tab";

/** Settings · Security (recent sign-ins). */
export default function SettingsSecurityPage() {
  return <SecuritySettingsTab />;
}
