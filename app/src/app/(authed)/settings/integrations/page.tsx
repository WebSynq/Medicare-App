"use client";

import { IntegrationsSettingsTab } from "@/components/settings/integrations-tab";

/** Settings · Integrations (GHL connect / disconnect). */
export default function SettingsIntegrationsPage() {
  return <IntegrationsSettingsTab />;
}
