# Security upgrade pass (2026-05). fastapi 0.110.1 capped starlette at
# <0.38.0 which left us on a CVE-affected line. Bumped fastapi to 0.116.2
# so we can pin starlette to 0.47.2. The rest of the stack pins to the
# security-baseline versions Tim's SCA scan requires.
fastapi==0.116.2
starlette==0.47.2
uvicorn[standard]==0.25.0
motor==3.3.1
pymongo==4.6.3
pydantic[email]>=2.6.4
python-dotenv>=1.0.1
python-multipart>=0.0.9
bcrypt==4.1.3
PyJWT==2.12.0
cryptography==46.0.6
httpx==0.28.1
reportlab>=4.0.0,<5
sentry-sdk[fastapi]==2.19.2
slowapi==0.1.9
limits>=3.10,<5
anthropic>=0.40.0
boto3>=1.34.0
openpyxl>=3.1.0
pandas>=2.0.0
apscheduler>=3.10.4,<4
tzlocal>=5.2,<6
resend>=2.0.0
# Google Calendar two-way sync (per-agent OAuth)
google-auth>=2.0.0
google-auth-oauthlib>=1.0.0
google-api-python-client>=2.0.0
# .ics export (download-to-calendar from appointment row)
icalendar>=5.0.0

pyotp
# Multi-tenant billing (Phase 3) — webhook signature verification +
# customer / subscription / invoice management. 15.2.0 is the latest
# stable as of 2026-05; pin major-version-floor so security patches
# flow without surprise API breakage.
stripe>=15.0.0,<16
